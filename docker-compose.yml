services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: maliscope
      POSTGRES_USER: maliscope
      POSTGRES_PASSWORD: maliscope
    ports:
      - "5432:5432"
    volumes:
      - maliscope-postgres:/var/lib/postgresql/data

volumes:
  maliscope-postgres:
