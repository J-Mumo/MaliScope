import { DiscoveryDashboard } from "@/components/discovery-dashboard";

export const metadata = {
  title: "Listings discovery | MaliScope",
};

export default function DiscoveryPage() {
  return <DiscoveryDashboard />;
}
